namespace Utilities
{
    /// <summary>
    /// Add this interface to all SingletonMonoBehaviours you don't want stacked under PersistentContainer (if enabled).
    /// </summary>
    public interface IPersistentContainerIgnore
    {
    }
}
